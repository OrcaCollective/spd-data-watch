version: "3"

services:
    app:
        build: .
        environment:
            FLASK_ENV: development
            FLASK_DEBUG: 1
        command: python3 run.py
        volumes:
            - ./app:/app/app